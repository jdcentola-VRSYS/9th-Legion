# Legion — The People’s Cloud

Legion is a decentralized compute grid that lets households contribute compute to paid AI/HPC jobs and reuse the heat locally.

## Milestone A
- Create basic repo layout
- Open Codespaces
- Show a heartbeat demo on a tiny web dashboard
